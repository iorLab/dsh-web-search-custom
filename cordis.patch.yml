# Community bundle patch: insert the DIY web-search provider into the
# browser roster when a profile lists this package. The row name is the
# package itself, so the client-modules node half resolves its `dsh.client`
# declaration and serves /plugins/<name>/client.js.
- insert:
    - id: dsh-web-search-diy
      name: dsh-web-search-diy

# Community bundle patch: insert the Custom web-search provider into the
# browser roster when a profile lists this package, and select it as the web
# seam's active search provider.
- insert:
    - id: dsh-web-search-custom
      name: dsh-web-search-custom
- override:
    plugins.web.searchProvider: custom-responses
